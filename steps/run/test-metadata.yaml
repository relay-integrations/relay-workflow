# connections:
#   aws/test:
#     accessKeyID: AKIMYKEY
#     secretAccessKey: mySecret


runs:
  '1':
    steps:
      default:
        name: run
        image: workflow-run
        spec:
          name: some-workflow
          aap: noot